# repo branches

branches:
  only:
  - master

# job environment

sudo: false

language: python

python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"

# job phases

install:
  - pip install --editable .[test] .

script:
  - pylint stargazers
  - cd ./tests
  - py.test -vv --cov=stargazers --cov-branch --cov-report=term-missing --cov-config=.coveragerc
